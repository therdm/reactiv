name: reactiv
description: Production-ready reactive state management with robust logging, undo/redo, computed values, debounce/throttle, and more. Lightweight, powerful, and easy to use.
version: 1.1.0
homepage: https://github.com/therdm/reactiv
repository: https://github.com/therdm/reactiv
issue_tracker: https://github.com/therdm/reactiv/issues

topics:
  - reactive
  - state-management
  - flutter-package
  - dependency-injection
  - mvvm

environment:
  sdk: '>=2.19.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

