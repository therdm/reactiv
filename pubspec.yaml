name: reactiv
description: Reactive state management approach and dependency injection  inspired by GetX
version: 0.2.7
homepage: https://github.com/therdm/reactiv

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"


dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

