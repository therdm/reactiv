name: reactiv
description: Light-weight Reactive state management approach tool and dependency injection tool for flutter available for all platforms
version: 0.3.4
homepage: https://github.com/therdm/reactiv

environment:
  sdk: '>=2.19.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

